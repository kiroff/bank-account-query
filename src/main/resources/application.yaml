server:
  port: 5001

spring:
  application:
    name: bank_query
  main:
    banner-mode: off

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  datasource:
    username: admin
    password: q
    url: jdbc:postgresql://localhost:5432/appdb
    driver-class-name: org.postgresql.Driver

  kafka:
    bootstrap-servers: localhost:9092,localhost:9094,localhost:9096
    topic: bank-account-events
    listener:
      ack-mode: MANUAL_IMMEDIATE
      poll-timeout: 900000
    consumer:
      group-id: bank-account-consumer
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: org.kiroff.bank.account.common.events,org.kiroff.bank.cqrs.core.events
          deserializer:
            value:
              delegate:
                class: org.springframework.kafka.support.serializer.JacksonJsonDeserializer
